name: Dynamic Quotes

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  update-quote:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Update Dynamic Quote
        run: |
          echo "Daily quote refreshed!"
          echo "Note: Quotes are automatically generated by the Vercel API in README"
